



 <% if @question.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@question.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @question.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<!DOCTYPE html>
<html lang="en">
<head>
  
  <title>New Question</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
</head>

<body>
<%= form_with scope: :question, url: questions_path, local: true do |form| %>
<div class="container">
  <form action="/action_page.php">
    <div class="form-group">
      <h2> New Question </h2>
      <label for="title">Title:</label>
      <input type="titleinput" class="form-control" id="title" placeholder="Enter title" name="title">
    </div>
  </form>
</div>

<div class="container"> 
  <form>
    <div class="form-group">
      <label for="Content">Content:</label>
      <textarea class="form-control" rows="5" id="Content"></textarea>
    </div>
      <button type="Post Question" class="btn btn-success">Post question</button>
      <p>
      <button type="Return to main page" class="btn btn-default">Cancel</button>
  </form>
</div>

  <p>
    <%= form.label :title %><br>
    <%= form.text_field :title %>
  </p>
 
  <p>
    <%= form.label :text %><br>
    <%= form.text_area :text %>
  </p>
 
  <p>
    <!--Not good yet since the message will appear even if a text has been entered.-->
    <!--, method: :get, data: { confirm: 'Please enter a question.' }-->
    <%= form.submit 'Post Question'%>
  </p>
<% end %>

<!--Added the back button to give the user an option to go back to the main page if he doesn't want to write a question anymore-->
<%= button_to 'Back', questions_path, method: :get%>


</body>
</html>

